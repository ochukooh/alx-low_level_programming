#include "man.h"

/**
 * *_strcat - concatenates two strings.
 * @dest - first strinh
 * @src - second string
 * Return: dest
 */
char *_strcat(char *dest, char *src)
{
	*dest[] = "hello";
	*src[] = "world";

	*_strcat(*dest, *src);
	return(*dest);
}
